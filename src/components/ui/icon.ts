/** Shared Lucide props — hairline strokes match the reference's custom SVGs (reference §8). */
export const iconProps = { size: 16, strokeWidth: 1.5, 'aria-hidden': true } as const;
export const smallIconProps = { size: 14, strokeWidth: 1.5, 'aria-hidden': true } as const;
